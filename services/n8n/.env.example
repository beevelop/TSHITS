# n8n Configuration
# See: https://docs.n8n.io/hosting/configuration/environment-variables/

# Domain for n8n access (required for webhooks)
SERVICE_DOMAIN=n8n.example.com

# Database credentials
DB_USER=n8n
DB_NAME=n8n
DB_PASS=Swordfish

# CRITICAL: Encryption key for credentials storage
# Generate with: openssl rand -base64 32
# WARNING: Loss of this key = loss of all stored credentials
N8N_ENCRYPTION_KEY=generate_with_openssl_rand_base64_32

# Timezone for workflow scheduling (IANA format)
TZ=UTC
