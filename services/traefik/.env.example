# Traefik Configuration
# =====================

# Mode: 'exposed' (default) or 'tunnel'
#   exposed - Direct port exposure with Let's Encrypt via CloudFlare DNS-01
#   tunnel  - Behind cloudflared, TLS terminated at Cloudflare edge
TRAEFIK_MODE=exposed

# Dashboard
TRAEFIK_DOMAIN=traefik.example.com
TRAEFIK_AUTH=bee:$2y$10$78FQgeJbUX8gBGu9RhVcSuoJFxknuIW63Z1.U97Qx59G58afEIrIu

# Email for Let's Encrypt notifications (exposed mode only)
TRAEFIK_EMAIL=admin@example.com

# CloudFlare credentials (exposed mode only)
CLOUDFLARE_EMAIL=user@example.com
CLOUDFLARE_API_KEY=your_cloudflare_api_key_here

# Optional: Network binding
# For tunnel mode, set these to 127.0.0.1 to restrict access
# TRAEFIK_BIND_IP=127.0.0.1
# TRAEFIK_DASHBOARD_BIND=127.0.0.1
