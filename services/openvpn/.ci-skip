# This service cannot be tested in CI
# Reason: Requires PKI initialization which generates certificates interactively.
# Without valid PKI in the openvpn_conf volume, the container will exit immediately.
