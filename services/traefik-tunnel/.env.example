# Traefik Tunnel Configuration
# ============================
# No CloudFlare API credentials needed - TLS terminated at Cloudflare edge

# Optional: Dashboard configuration (accessible via Cloudflare Tunnel)
TRAEFIK_DOMAIN=traefik.example.com
TRAEFIK_AUTH=admin:$apr1$changeme
