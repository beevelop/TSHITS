name: dependency-track
services:
  dtrack:
    image: dependencytrack/bundled:${DEPENDENCY_TRACK_VERSION:-4.13.6}
    container_name: dtrack
    volumes:
      - dtrack_data:/data
    networks:
      - traefik
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/version" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    logging:
      driver: "json-file"
      options:
        max-size: "500k"
        max-file: "50"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dtrack.rule=Host(`${SERVICE_DOMAIN:-example.com}`)"
      - "traefik.http.routers.dtrack.entrypoints=websecure"
      - "traefik.http.routers.dtrack.tls=true"
      - "traefik.http.routers.dtrack.tls.certresolver=letsencrypt"
      - "traefik.http.services.dtrack.loadbalancer.server.port=8080"
      - "traefik.docker.network=traefik_default"

networks:
  traefik:
    name: traefik_default
    external: true

volumes:
  dtrack_data:
    name: ${COMPOSE_PROJECT_NAME:-dependency-track}_app_data
